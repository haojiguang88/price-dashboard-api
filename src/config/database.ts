import sqlite3 from "sqlite3"; import { open } from "sqlite"; const dbPath = "./db/price_dashboard.db"; let dbInstance = null; const getDb = async () => { if (!dbInstance) { dbInstance = await open({ filename: dbPath, driver: sqlite3.Database }); await dbInstance.exec("CREATE TABLE IF NOT EXISTS price_records (id INTEGER PRIMARY KEY AUTOINCREMENT, date TEXT NOT NULL, category TEXT NOT NULL, object_name TEXT NOT NULL, variant TEXT, price REAL NOT NULL, source TEXT, note TEXT, created_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP, updated_at TEXT NOT NULL DEFAULT CURRENT_TIMESTAMP)"); console.log("SQLite database connected and table created if not exists"); } return dbInstance; }; export default getDb;
